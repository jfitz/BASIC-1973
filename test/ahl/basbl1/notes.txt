basbal1.bas

Why does BASIC-1973 not run this program?

This program uses the unary $ operator to input single characters from the console.
The unary operator reads characters without waiting for a newline.
It also converts characters to numeric, the same as the ASC() function.
