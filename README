BASIC-1973

This is an interpreter for a BASIC interpreter. Created in 1964 by Kemeny and Kurtz, BASIC was an interactive language that could take advantage of timesharing systems.

This version is a form of BASIC that was popular in the 1970s.

- Statements REM, DIM, LET, READ, DATA, PRINT, INPUT, IF, GOTO, GOSUB, RETURN, FOR, NEXT, MAT READ, MAT PRINT, and MAT
- The LET statement must include the LET keyword
- Functions ABS, SGN, SIN, COS, TAN, ATN, EXP, LOG, RND, ZER, CON, IDN, TRN, and INV
- User-defined functions in form FNx(X)
- Only one statement on each line
- No file operations
- No IF/THEN/ELSE. IF/THEN is supported, and that only with a target line number, not a target statement
- Numeric variables only; no string variables
- Interactive mode and command-line mode
- Trace ability to print lines as they are executed
- Option for slow output to simulate Teletype

